/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "charingcub.site",
      "127.0.0.1",
      "api.sandbox.midtrans.com",
      "dev.charingcub.site",
    ],
  },
};

export default nextConfig;
